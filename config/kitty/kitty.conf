# vim:fileencoding=utf-8:ft=conf:foldmethod=marker

shell                   /usr/bin/tmux
editor                  .

# Font config
font_size 							10.0
font_family       			JetBrainsMono-Regular
bold_font         			JetBrainsMono-Bold
italic_font       			JetBrainsMono-Italic
bold_italic_font  			JetBrainsMono-BoldItalic

# Window size/behaviour
remember_window_size 		no
initial_window_width 		81c
initial_window_height 	24c

# ~ * c o l o u r s * ~
include                 colorscheme.monokai-soda.conf
background_opacity      1.0

# Tab navigation shortcut keys similar to gui applications
map alt+1 							goto_tab 1
map alt+2 							goto_tab 2
map alt+3 							goto_tab 3
map alt+4 							goto_tab 4
map alt+5 							goto_tab 5
map alt+6 							goto_tab 6
map alt+7 							goto_tab 7
map alt+8 							goto_tab 8
map alt+9 							goto_tab 9

# OS-specific stuff

# Do not keep the application open after the last window is closed in macOS
# (i.e. allow ^D spam to actually quit everything)
macos_quit_when_last_window_closed  yes

# Don't show the window title in the menu bar
macos_show_window_title_in          window
